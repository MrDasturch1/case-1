'use strict'




let natija,
    x = 1;

switch (x) {
    case 1:

        console.log(natija = `Bir yuz `);

        break;
    case 2:

        console.log(natija = `Ikki yuz `);

        break;
    case 3:

        console.log(natija = `Uch yuz `);

        break;
    case 4:

        console.log(natija = `To'rt yuz `);

        break;
    case 5:

        console.log(natija = `Besh yuz `);

        break;
    case 6:

        console.log(natija = `Olti yuz `);

        break;
    case 7:

        console.log(natija = `Yetti yuz `);

        break;
    case 8:

        console.log(natija = `Sakkiz yuz `);

        break;
    case 9:

        console.log(natija = `To'qqiz yuz `);

        break;
    default:
        console.log(`Xato`);

        break;
}
console.log(
    x
);